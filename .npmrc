# ⚠ BUILD-Д ХЭРЭГТЭЙ. Railway дээр `NODE_ENV=production` тавигдсан тул
# `npm ci` нь devDependencies-ийг АЛГАСДАГ. Гэтэл `nest` (build хийдэг)
# нь devDependency — ингээд build нь `nest: not found` гэж унадаг байв.
#
# Энэ мөр NODE_ENV-ээс үл хамааран dev багцуудыг суулгана.
include=dev
